{
    "scripts": {
        "dev": "pwsh -NoProfile -ExecutionPolicy Bypass -File ../../scripts/dev-api.ps1",
        "dev:go-run": "go run ./cmd/main.go",
        "test": "go test ./...",
        "test:clean": "go clean -testcache && go test ./...",
        "test:withLogs": "go test ./... -v",
        "test:clean:withLogs": "go clean -testcache && go test ./... -v",
        "refresh:bungie-manifest": "go run ./internal/domains/bungie/cmd/refresh_manifest"
    },
    "name": "apps",
    "private": true,
    "version": "0.0.0"
}
