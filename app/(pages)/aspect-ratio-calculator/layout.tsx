export const metadata = {
    title: 'Aspect Ratio Calculator | Lesi',
    description: 'Calculate the aspect ratio of an image',
};

export default function PageLayout({ children }: { children: React.ReactNode }) {
    return <div>{children}</div>;
}
